# Backend API base URL. Leave empty for same-origin (e.g. when frontend and API are served together).
# Desktop: use localhost. Phone on same Wi‑Fi: use your computer's local IP.
# NEXT_PUBLIC_API_BASE=http://localhost:8000
# NEXT_PUBLIC_API_BASE=http://192.168.1.XX:8000
