import React from "react";
import { PlaceholderScreen } from "./shared";

export function ImportScreen() {
  return (
    <PlaceholderScreen
      title="Import"
      description="Import flow is scaffolded for mobile and can be wired to transcript/link parsing in the next slice."
    />
  );
}
